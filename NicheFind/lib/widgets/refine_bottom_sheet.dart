import 'package:flutter/material.dart';

class RefineBottomSheet extends StatelessWidget {
  final Function(String) onSubmit;

  const RefineBottomSheet({super.key, required this.onSubmit});

  @override
  Widget build(BuildContext context) {
    // TODO: Bottom sheet with text field for refinement feedback
    // e.g. "make them more beginner-friendly", "focus on physical products"
    throw UnimplementedError();
  }
}
