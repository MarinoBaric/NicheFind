import 'package:flutter/material.dart';
import '../models/niche_suggestion.dart';

class NicheCard extends StatelessWidget {
  final NicheSuggestion suggestion;

  const NicheCard({super.key, required this.suggestion});

  @override
  Widget build(BuildContext context) {
    // TODO: Build card with title, description, demand/competition badges, expandable first steps
    throw UnimplementedError();
  }
}
