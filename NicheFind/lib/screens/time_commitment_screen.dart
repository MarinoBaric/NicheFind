import 'package:flutter/material.dart';

class TimeCommitmentScreen extends StatelessWidget {
  const TimeCommitmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Step 4 of 5 - Time commitment selection
    // Options: 1-3h (A few hours), 3-5h (Part-time hobby), 5-10h (Dedicated time),
    //          10-20h (Serious commitment), 20+h (Full focus)
    // Show selected value at bottom: "Selected: X hours/week"
    // Back / Next navigation
    throw UnimplementedError();
  }
}
