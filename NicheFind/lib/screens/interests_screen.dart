import 'package:flutter/material.dart';

class InterestsScreen extends StatelessWidget {
  const InterestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Step 1 of 5 - Interest selection with chips + custom input
    // Chips: Fitness, Tech, Cooking, Finance, Gaming, Art, Travel, Fashion, Education, Health, Music, Business
    // "Add custom interest" text field with add button
    // Back / Next navigation
    throw UnimplementedError();
  }
}
