name: niche_find
description: An AI-powered niche discovery tool built around the DeepSeek API.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  provider: ^6.1.0
  flutter_markdown: ^0.7.6
  share_plus: ^10.1.0
  url_launcher: ^6.3.1
  connectivity_plus: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
